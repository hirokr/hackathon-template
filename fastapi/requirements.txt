# requirements.txt — managed via uv
# Install: uv pip install -r requirements.txt

fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.29.0           # async PostgreSQL driver
psycopg2-binary==2.9.9    # sync driver (for Alembic)
pydantic==2.7.1
pydantic-settings==2.2.1
passlib[bcrypt]==1.7.4
itsdangerous==2.2.0       # session signing
starlette==0.37.2
python-multipart==0.0.9
authlib==1.3.0            # OAuth (Google + Meta)
httpx==0.27.0             # used by authlib
python-dotenv==1.0.1
