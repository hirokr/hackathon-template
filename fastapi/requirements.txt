# requirements.txt — managed via uv
# Install: uv pip install -r requirements.txt

fastapi==0.111.0 
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.31.0           # async PostgreSQL driver (CPython 3.13 compatible)
pydantic==2.13.4
pydantic-settings==2.14.1
passlib[bcrypt]==1.7.4
itsdangerous==2.2.0       # session signing
starlette==0.37.2
python-multipart==0.0.9
authlib==1.3.0            # OAuth (Google + Meta)
httpx==0.27.0             # used by authlib
python-dotenv==1.0.1
python-jose[cryptography]==3.3.0  # JWT signing/verification
celery[redis]==5.4.0      # background tasks
redis==5.0.4              # celery broker/backend
